import React from "react";
import HeroContent from "../sub/HeroContent";

const Hero = () => {
  return (
    <div className="relative w-full" id="Hero">
      <HeroContent />
    </div>
  );
};

export default Hero;
